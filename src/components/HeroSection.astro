---
const { title, avatar } = Astro.props;

const words = title.split(" ");
const lastWord = words.pop();
const rest = words.join(" ");
---

<div class="text-center space-y-5 text-sm md:text-base">
  <img
    class="w-40 md:w-52 rounded-full mx-auto ring-2 ring-violet-400/40 drop-shadow-lg animate-float"
    src={avatar}
    alt={title}
  />
  <h1 class="text-3xl md:text-5xl font-bold drop-shadow-md">
    {rest}
    <span class="text-violet-400">{lastWord}</span>
  </h1>

  <p class="text-sm md:text-base leading-relaxed opacity-80 max-w-sm mx-auto">
    A corner for projects, experiments, and notes left behind.
  </p>

  <!-- <div class="mt-8 flex flex-col gap-3 sm:flex-row sm:justify-center">
    <button
      type="button"
      data-archive-view="works"
      class="rounded-full bg-violet-400/40 px-6 py-2 font-bold transition hover:scale-105 hover:bg-violet-400/50"
    >
      Explore Works
    </button>
    <button
      type="button"
      data-archive-view="notes"
      class="rounded-full bg-violet-400/15 px-6 py-2 font-bold transition hover:scale-105 hover:bg-violet-400/25"
    >
      Read Notes
    </button>
  </div> -->
</div>